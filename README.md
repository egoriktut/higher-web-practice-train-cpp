# Sorting Hill

Проект «Модель железной дороги»

### Build and run railway
```bash
g++ train/*.cpp -o railway && ./railway
```

### Build and run tests
```bash
g++ tests/test.cpp  train/sorting_*.cpp -o test && ./test
```